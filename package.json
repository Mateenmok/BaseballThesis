{
  "name": "baseball-thesis",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:data": "node scripts/build-player-data.mjs",
    "build:profiles": "node scripts/build-player-profiles.mjs",
    "build:metrics": "node scripts/build-leadoff-metrics.mjs",
    "build": "npm run build:data && npm run build:profiles && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "read-excel-file": "^9.3.10",
    "typescript": "latest",
    "vite": "latest"
  }
}
